# Workspace-wide pnpm configuration. Kept minimal on purpose.
# Resolve workspace packages via the workspace: protocol, not the registry.
link-workspace-packages=true
# Hoisted layout required for Expo monorepo support (https://docs.expo.dev/guides/monorepos/).
node-linker=hoisted
